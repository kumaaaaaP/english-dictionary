name: Generate Vocabulary HTML Files
# vocabulary_data*.json または data/ が変更されたときに実行
on:
  push:
    branches:
      - main
    paths:
      - 'vocabulary_data*.json'
      - 'data/**'
      - 'build_index.py'
      - 'generate_vocab.py'
  workflow_dispatch:  # 手動実行も可能

jobs:
  generate:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      # 1. リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # 2. Pythonをセットアップ
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      # 3. dataフォルダを作成（存在しない場合）
      - name: Create data directory
        run: mkdir -p data
      
      # 4. 単語HTMLファイルを生成（JSONファイルが変更された場合のみ）
      - name: Check if JSON files changed
        id: check_json
        run: |
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -q "vocabulary_data.*\.json"; then
            echo "json_changed=true" >> $GITHUB_OUTPUT
          else
            echo "json_changed=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Generate vocabulary HTML files
        if: steps.check_json.outputs.json_changed == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          python generate_vocab.py
          echo "✅ Vocabulary HTML files generated"
      
      # 5. index.htmlを更新（build_index.py を使用）
      - name: Update index.html
        run: |
          python build_index.py
          echo "✅ index.html updated"
      
      # 6. 変更があるかチェック
      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet data/ index.html; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No changes detected"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ Changes detected"
          fi
      
      # 7. 生成されたファイルをコミット＆プッシュ
      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/*.html index.html
          git commit -m "🤖 Auto-generate vocabulary HTML files and update index [skip ci]"
          git push
      
      # 8. 完了サマリー
      - name: Job summary
        run: |
          echo "## 📊 Generation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### JSON Files Detected" >> $GITHUB_STEP_SUMMARY
          ls -1 vocabulary_data*.json 2>/dev/null | while read file; do
            count=$(jq '.words | length' "$file" 2>/dev/null || echo "0")
            echo "- \`$file\`: $count words" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Vocabulary HTML files generated in \`data/\` directory" >> $GITHUB_STEP_SUMMARY
          echo "✅ \`index.html\` updated with new words" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Generated Files" >> $GITHUB_STEP_SUMMARY
          ls -1 data/*.html 2>/dev/null | wc -l | xargs -I {} echo "{} HTML files in data/" >> $GITHUB_STEP_SUMMARY

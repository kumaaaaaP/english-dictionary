name: Generate Vocabulary HTML Files

# いつ実行するか
on:
  push:
    branches:
      - main  # mainブランチにプッシュされたとき
    paths:
      - 'vocabulary_data.json'  # このファイルが変更されたときのみ実行
  workflow_dispatch:  # 手動実行も可能

jobs:
  generate:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # リポジトリへの書き込み権限
    
    steps:
      # 1. リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # 2. Pythonをセットアップ
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      # 3. HTMLファイルを生成
      - name: Generate HTML files
        run: |
          echo "🔨 HTMLファイルを生成中..."
          python generate_vocab.py
          echo "✅ 生成完了"
          echo ""
          echo "📁 dataフォルダの内容:"
          ls -la data/ || echo "dataフォルダが見つかりません"
      
      # 4. 生成されたファイルをステージング
      - name: Stage generated files
        run: |
          if [ -d "data" ]; then
            git add data/
            echo "✅ dataフォルダをステージングしました"
          else
            echo "⚠️ dataフォルダが存在しません"
            exit 1
          fi
      
      # 5. 変更があるかチェック
      - name: Check for changes
        id: check_changes
        run: |
          if git diff --cached --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ 変更はありません"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ 変更が検出されました"
            git diff --cached --stat
          fi
      
      # 6. 生成されたファイルをコミット＆プッシュ
      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -m "🤖 Auto-generate vocabulary HTML files"
          git push
          echo "✅ 変更をプッシュしました"
      
      # 7. 完了メッセージ
      - name: Summary
        run: |
          echo "✅ HTML generation completed!"
          if [ -d "data" ]; then
            echo "📊 生成されたファイル数: $(ls data/*.html 2>/dev/null | wc -l)"
            echo ""
            echo "📁 生成されたファイル一覧:"
            ls -1 data/*.html 2>/dev/null || echo "なし"
          fi
